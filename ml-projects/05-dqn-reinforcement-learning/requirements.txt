torch>=2.0
gymnasium[classic-control]>=0.29
numpy>=1.24
matplotlib>=3.7

# Optional: LunarLander-v3 needs the Box2D physics extra, which in turn
# requires the SWIG system package (e.g. `apt install swig` / `brew install swig`):
#   pip install "gymnasium[box2d]"

# Dev / tests
pytest>=7
